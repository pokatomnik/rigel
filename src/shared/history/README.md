# History

This module manages conversation history. It provides the persistent history
store and the synchronization layer that keeps in-memory messages and saved
messages consistent during a model turn.
