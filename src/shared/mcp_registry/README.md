# MCP Registry

This module loads configured Model Context Protocol servers and registers their
tools with Rigel agents. It contains the server configuration types and the
runtime registry for HTTP and standard-input/output MCP connections.
